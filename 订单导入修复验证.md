# 订单导入修复验证指南

## ✅ 已修复问题

### 问题：订单导入提示 "Unknown column 'project_id' in 'field list'"

**修复内容：**
1. ✅ 添加 project_id 字段到 orders 表
2. ✅ 更新现有订单的 project_id 数据
3. ✅ 添加 project_id 索引
4. ✅ 修改导入导出代码包含该字段
5. ✅ 重启后端服务

---

## 🧪 测试步骤

### 步骤1：准备测试数据

创建一个Excel文件（如 test_orders.xlsx），包含以下列：

| 订单编号 | 订单日期 | 合同编号 | 项目名称 | 客户名称 | 公司 | 业务类别 | 数量 | 单价 | 合计 |
|---------|---------|---------|---------|---------|-----|---------|------|------|------|
| TEST001 | 2024-05-18 | HT001 | 测试项目 | 测试客户 | 华为 | 软件开发 | 1 | 10000 | 10000 |
| TEST002 | 2024-05-18 | HT002 | 测试项目2 | 测试客户2 | 中兴 | 系统集成 | 2 | 5000 | 10000 |

### 步骤2：执行导入测试

1. 打开应用：http://localhost:5173
2. 进入：业务管理 → 订单管理
3. 点击：导入导出按钮 → 导入
4. 选择：准备好的Excel文件
5. 点击：确认导入

### 步骤3：验证导入结果

**预期结果：**
- ✅ 导入成功，显示成功记录数
- ✅ 订单列表中显示新导入的订单
- ✅ 订单详情中 project_id 和 project_name 正确
- ✅ 订单详情中 company_id 和 company_name 正确

---

## 🔍 故障排查

### 如果导入仍然失败：

**1. 检查后端日志**
```bash
cd backend
tail -50 backend.log
```

**2. 检查数据库字段**
```sql
DESCRIBE orders;
-- 应该看到 project_id 字段
```

**3. 检查服务状态**
```bash
netstat -ano | grep :3001
-- 应该显示服务正在监听
```

**4. 清除浏览器缓存**
```
Ctrl + Shift + R (强制刷新)
或 Ctrl + F5
```

---

## 📊 数据验证SQL

**检查订单的 project_id 是否正确填充：**
```sql
SELECT 
    o.id,
    o.order_no,
    o.contract_id,
    o.contract_no,
    o.project_id,
    o.project_name,
    c.project_id AS contract_project_id
FROM orders o
LEFT JOIN contracts c ON c.id = o.contract_id
WHERE o.project_id IS NULL 
  AND o.contract_id IS NOT NULL
LIMIT 10;
```

**检查订单的公司信息：**
```sql
SELECT 
    id,
    order_no,
    company_id,
    company_name,
    project_id,
    project_name
FROM orders
ORDER BY id DESC
LIMIT 10;
```

---

## ⚠️ 注意事项

### 导入字段说明

**必需字段：**
- 订单编号（order_no）
- 订单日期（date）
- 合同编号（contract_no）或合同名称

**自动填充字段：**
- project_id - 从合同自动获取
- project_name - 从合同或项目自动获取
- company_id - 从合同自动获取
- company_name - 从合同自动获取
- customer_id - 从合同自动获取
- customer_name - 从合同自动获取

### 导入规则

1. **合同编号匹配：**
   - 如果提供合同编号，系统自动查找对应合同
   - 从合同继承：项目、公司、客户、部门等信息

2. **项目名称处理：**
   - 优先使用Excel中的项目名称
   - 如果为空，从合同关联的项目自动获取

3. **公司信息：**
   - 可以在Excel中指定不同的公司
   - 如果不指定，从合同自动获取

---

## 🎯 验证清单

- [ ] 后端服务正常运行（端口3001）
- [ ] 前端服务正常运行（端口5173）
- [ ] 数据库 orders 表有 project_id 字段
- [ ] 导入测试文件准备完成
- [ ] 导入功能测试成功
- [ ] 订单详情显示正确
- [ ] 公司、项目信息正确

---

## 📞 如有问题

如果导入仍然失败，请提供：
1. 后端日志（backend.log 最后50行）
2. 浏览器控制台错误信息
3. 测试导入的Excel文件内容
4. 数据库字段检查结果
