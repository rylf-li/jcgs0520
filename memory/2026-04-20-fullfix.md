# 2026-04-20 订单导入全面修复

## 完成情况

### ✅ 后端已全面恢复并运行
- 后端运行在端口 3001（PID 动态）
- 所有 18 个模型文件重建完成
- 所有 14 个路由文件创建/恢复
- 缺失的 middleware/auth.js、utils/helpers.js、utils/syncAmount.js 均已创建
- .env 路径修复（app.js 指向 backend/.env）
- models/index.js 清理了不存在的统计表引用

### ✅ 导入核心问题已修复

**1. 语法错误（多处缺少逗号）**
- importExport.js 的 FIELD_CONFIG 中 5 处数组闭合后缺少逗号
- 已全部修复

**2. 字段配置与数据库不匹配**
- contracts.project_name lookup：DB 无此列 → 已移除 lookup 配置
- contracts.bank_account_name lookup：DB 无此列 → 已移除 lookup 配置
- orders 字段配置全面重写，匹配 DB 实际列名：
  - order_no, date, contract_no (lookup→contract_id), project_name, customer_name
  - business_category, business_parameter, business_unit, business_project
  - quantity, unit_price, total, report_date, report_no
  - settlement_fee, performance_fee, manager, salesman, dept_name, company_name, status, remark

**3. 性能优化（Phase1 + Phase2）**
- Phase1 预加载所有已有订单到 orderMap → O(1) 去重查找
- Phase1 预加载所有合同到 contractMap
- orders 导入时自动回填 contract_id（通过 contractMap）
- Phase2 bulkCreate 使用 updateOnDuplicate（避免重复 key 报错）

**4. 业务明细导入**
- 数据库 business 表不存在 → 已通过 SQL CREATE TABLE 创建
- 字段：id, name, category, unit, unit_price, description, remark（匹配 FIELD_CONFIG）

### ⚠️ 仍有待验证
1. 导入超时问题：优化后理论上不会超时，需实际测试（前端上传 Excel 文件测试）
2. updateOnDuplicate 字段列表是否完整（已包含所有 orders DB 列）
3. auth 路由的密码验证是简单比较（临时方案）

### 关键文件路径
- 后端入口：`backend/src/app.js`
- 导入逻辑：`backend/src/routes/importExport.js`
- 业务表创建：`backend/src/models/Business.js`
- 认证中间件：`backend/src/middleware/auth.js`
- 同步工具：`backend/src/utils/syncAmount.js`
